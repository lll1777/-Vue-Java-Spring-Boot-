package com.ticket.management.enums;

public enum TicketStatus {
    DRAFT,
    PENDING_APPROVAL,
    APPROVED,
    REJECTED,
    ASSIGNED,
    IN_PROGRESS,
    PENDING_REVIEW,
    RESOLVED,
    CLOSED,
    CANCELLED,
    ESCALATED
}
